apiVersion: v1
kind: Service
metadata:
  name: sandbox-runner
  namespace: sandbox
spec:
  clusterIP: None
  selector:
    app: sandbox-runner
  ports:
    - name: http
      port: 8080
      targetPort: 8080
